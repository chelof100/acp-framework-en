\* TLC model-checking configuration for ACP.tla
\* Run: java -jar tla2tools.jar -config ACP.cfg ACP.tla

SPECIFICATION
    Spec

\* Finite model constants
\* 2 agents × 4 capabilities × 3 resources = 24 distinct request types
\* With ledger bound = 5, state space is tractable for TLC
CONSTANTS
    Agents       = {"A1", "A2"}
    Capabilities = {"read", "write", "financial", "admin"}
    Resources    = {"public", "sensitive", "restricted"}

\* State invariants — checked on every reachable state
INVARIANTS
    TypeInvariant
    Safety
    LedgerAppendOnly
    RiskDeterminism

\* Temporal properties — checked over all traces
PROPERTIES
    LedgerAppendOnlyTemporal
